<UserControl x:Class="Axon.UI.Components.AxonTabControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             x:Name="AxonTabControlRoot">

    <UserControl.Resources>

        <!-- Estilo para los botones de Tab -->
        <Style x:Key="AxonTabButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="Margin" Value="0,0,0,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Foreground" Value="{DynamicResource ColorNeutralForeground3Brush}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid x:Name="ButtonGrid" Background="{TemplateBinding Background}">
                            <ContentPresenter x:Name="ContentHost"
                                            Margin="{TemplateBinding Padding}"
                                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                            Content="{TemplateBinding Content}"
                                            ContentTemplate="{TemplateBinding ContentTemplate}"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource ColorNeutralForeground2Brush}"/>
                                <Setter TargetName="ButtonGrid" Property="Background" Value="{DynamicResource ColorSubtleBackgroundHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource ColorNeutralForeground1Brush}"/>
                                <Setter TargetName="ButtonGrid" Property="Background" Value="{DynamicResource ColorSubtleBackgroundPressedBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                    <Setter Property="Foreground" Value="{DynamicResource ColorBrandForeground1Brush}"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- Animaciones para el indicador -->
        <Storyboard x:Key="IndicatorMoveAnimation">
            <DoubleAnimation x:Name="IndicatorMoveX"
                           Storyboard.TargetName="SelectedIndicator"
                           Storyboard.TargetProperty="(Canvas.Left)"
                           Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation x:Name="IndicatorResizeWidth"
                           Storyboard.TargetName="SelectedIndicator"
                           Storyboard.TargetProperty="Width"
                           Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>

    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header con los tabs -->
        <Border Grid.Row="0" 
                Background="{DynamicResource ColorNeutralBackground1Brush}"
                BorderBrush="{DynamicResource ColorNeutralStroke3Brush}"
                BorderThickness="0,0,0,1">

            <Grid>
                <!-- Contenedor de botones de tab -->
                <ItemsControl x:Name="TabHeadersContainer"
                            ItemsSource="{Binding Items, ElementName=AxonTabControlRoot}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Style="{StaticResource AxonTabButtonStyle}"
                                    Content="{Binding Header}"
                                    Command="{Binding SelectTabCommand, ElementName=AxonTabControlRoot}"
                                    CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Canvas para el indicador animado -->
                <Canvas x:Name="IndicatorCanvas" 
                        VerticalAlignment="Bottom"
                        Height="3">
                    <Rectangle x:Name="SelectedIndicator"
                             Height="3"
                             Fill="{DynamicResource ColorBrandBackgroundBrush}"
                             RadiusX="1.5"
                             RadiusY="1.5"
                             Canvas.Left="0"
                             Width="80"/>
                </Canvas>
            </Grid>
        </Border>

        <!-- Contenido del tab seleccionado -->
        <ContentPresenter Grid.Row="1"
                         x:Name="TabContentPresenter"
                         Content="{Binding SelectedTabItem.Content, ElementName=AxonTabControlRoot}"
                         ContentTemplate="{Binding SelectedTabItem.ContentTemplate, ElementName=AxonTabControlRoot}"/>
    </Grid>
</UserControl>